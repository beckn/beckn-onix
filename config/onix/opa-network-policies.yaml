networkPolicies:
  default:
    # Example only. Replace this test policy path with your own policy before production use.
    type: file
    location: /app/pkg/plugin/implementation/opapolicychecker/testdata/example.rego
    query: data.policy.result
